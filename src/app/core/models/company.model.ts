export class Company {
  // 企業コード
  companyCode: string;
  // 企業名
  companyName: string;
  // 設立年月日
  establishmentDate: Date;
  // 上場区分
  stockMarket: string;
  // 業態
  businessCategory: string;
  // スコア
  score?: number;
  // ランク
  rank?: string;
}
